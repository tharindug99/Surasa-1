{
  "name": "surasa-01",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npm-run-all --parallel install-frontend install-backend start-client start-backend",
    "install-frontend": "cd frontend && npm install",
    "install-backend": "cd backend && composer install",
    "start-client": "cd frontend && npm start -- --host 0.0.0.0",
    "start-backend": "cd backend && php artisan serve --host 0.0.0.0"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@mui/icons-material": "^5.16.0",
    "concurrently": "^8.2.2",
    "react-slick": "^0.30.2",
    "slick-carousel": "^1.8.1"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
