{
  "name": "surasa-01",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npm-run-all --parallel install-frontend install-backend start-client start-backend",
    "install-frontend": "cd frontend && npm install",
    "install-backend": "cd backend && composer install",
    "start-client": "cd frontend && npm start",
    "start-backend": "cd backend && php artisan serve"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
